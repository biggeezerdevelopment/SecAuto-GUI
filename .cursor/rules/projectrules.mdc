---
description: Main Project Rule File 
globs: 
alwaysApply: True
---
utilize @web http://localhost:8080/docs to understand remote api calls to the backend server
always do a list of the project tree before any changes
# Front End
  go with gin and bootstrap and htmx and postgress database, but use sqlite for testing
   
#Project Structure for the project
project_root/
│
├── cmd/
│   └── main.go             // The entry point of the application
│
├── internal/
│   ├── server/
│   │   ├── server.go       // Server setup and configuration
│   │   └── routes.go       // Define routes and handlers
│   │
│   ├── handlers/
│   │   └── handlers.go     // Business logic for handling HTTP requests
│   │
│   └── models/
│       └── models.go       // Data models and database interactions
│
├── web/
│   ├── templates/
│   │   └── layout.html     // Base HTML template using Bootstrap and HTMX
│   │   └── index.html      // Main page
│   │
│   ├── static/
│   │   ├── css/
│   │   │   └── style.css   // Custom styles, including dark theme
│   │   ├── js/
│   │   │   └── main.js     // Optional JS for additional interactivity
│   │   └── images/
│   │       └── logo.png    // Static image files
│
├── go.mod                   // Go module file
└── go.sum                   // Dependencies checksum file